#ifndef SMOOTHLISTVIEW_H
#define SMOOTHLISTVIEW_H

#include <QListView> // Включаем базовый класс QListView.

/**
 * @class SmoothListView
 * @brief Кастомный класс QListView, обеспечивающий плавную попиксельную прокрутку.
 *
 * @details Этот класс наследуется от QListView и переопределяет его поведение,
 * связанное с прокруткой и компоновкой, для достижения более плавного и современного
 * пользовательского опыта, характерного для мобильных приложений и современных мессенджеров.
 *
 * Ключевые особенности:
 * 1.  Включает режим попиксельной прокрутки (`ScrollPerPixel`).
 * 2.  Снижает интенсивность прокрутки колесом мыши для более точного контроля.
 * 3.  Использует пакетную компоновку (`Batched` layout mode) для повышения производительности
 *     при работе с большим количеством элементов.
 * @author kex1tu
 */
class SmoothListView : public QListView
{
    Q_OBJECT // Обязательный макрос для классов Qt.

public:
    /**
     * @brief Конструктор.
     * @param parent Родительский виджет.
     */
    explicit SmoothListView(QWidget *parent = nullptr);

protected:
    /**
     * @brief Переопределенный обработчик событий колеса мыши.
     * @details Этот метод перехватывает стандартное событие прокрутки, уменьшает
     *          его интенсивность (`angleDelta`) и передает измененное событие
     *          в базовый класс, что приводит к более медленной и плавной прокрутке.
     * @param e Указатель на событие колеса мыши.
     */
    void wheelEvent(QWheelEvent *e) override;

    /**
     * @brief Переопределенный обработчик событий изменения размера.
     * @details Вызывает `doItemsLayout()` после изменения размера, чтобы принудительно
     *          пересчитать компоновку элементов. Это помогает избежать визуальных
     *          артефактов, которые могут возникнуть при использовании `ScrollPerPixel`.
     * @param e Указатель на событие изменения размера.
     */
    void resizeEvent(QResizeEvent *e) override;

    /**
     * @brief Переопределенный обработчик событий для области просмотра (viewport).
     * @details Используется для корректной обработки событий наведения мыши (`Hover`),
     *          чтобы делегат (`ChatMessageDelegate`) мог корректно отрисовывать
     *          hover-эффекты для элементов списка.
     * @param event Указатель на событие.
     * @return bool `true` если событие было обработано, иначе `false`.
     */
    bool viewportEvent(QEvent *event) override;
};

#endif // SMOOTHLISTVIEW_H
